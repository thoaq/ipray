# Kopieren nach .env und mit den Werten aus dem eigenen Supabase-Projekt füllen
# (Project Settings → API). Der anon key ist bewusst öffentlich nutzbar,
# Row Level Security schützt die Daten trotzdem — siehe supabase/migrations/0001_init.sql.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
